# Archived Build Reports

이 폴더는 `build/reports`에서 보존 기준(최신 1개 + milestone)을 벗어난 과거 산출 리포트를 저장합니다.

원칙:

- 원본 파일명은 변경하지 않습니다.
- 복원 시 `build/reports`로 이동해 재사용할 수 있어야 합니다.
- 분석 근거가 필요한 경우, 별도 요약은 `docs/reports/*.md`에 남깁니다.
