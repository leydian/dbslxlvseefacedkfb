<Window x:Class="WpfHost.CommandPalette"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="명령 팔레트"
        Width="520" Height="400"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        PreviewKeyDown="Window_PreviewKeyDown"
        Deactivated="Window_Deactivated">
    <Border Background="#F8F5FF"
            BorderBrush="#7C3AED"
            BorderThickness="1.5"
            CornerRadius="14"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Search box -->
            <Border Grid.Row="0"
                    Background="#FFFFFF"
                    BorderBrush="#C4B5FD"
                    BorderThickness="1.5"
                    CornerRadius="10"
                    Padding="10,6">
                <DockPanel>
                    <TextBlock Text="⌨ " DockPanel.Dock="Left" VerticalAlignment="Center" FontSize="16" Foreground="#7C3AED"/>
                    <TextBox x:Name="SearchTextBox"
                             BorderThickness="0"
                             Background="Transparent"
                             FontSize="14"
                             Foreground="#1E1B4B"
                             VerticalAlignment="Center"
                             TextChanged="SearchTextBox_TextChanged"
                             KeyDown="SearchTextBox_KeyDown"
                             Padding="4,2"/>
                </DockPanel>
            </Border>

            <!-- Command list -->
            <ListBox x:Name="CommandListBox"
                     Grid.Row="2"
                     Background="Transparent"
                     BorderThickness="0"
                     SelectionMode="Single"
                     KeyDown="CommandListBox_KeyDown"
                     MouseDoubleClick="CommandListBox_MouseDoubleClick"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,7"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="8"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#EDE9FE"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="#7C3AED"/>
                                            <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#F5F3FF"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.45"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- Footer hint -->
            <TextBlock Grid.Row="3"
                       Text="↑↓ 탐색   Enter 실행   Esc 닫기"
                       FontSize="11"
                       Foreground="#A899D4"
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0"/>
        </Grid>
    </Border>
</Window>
